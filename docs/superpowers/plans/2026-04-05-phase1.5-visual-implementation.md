# 药灵山谷 - Phase 1.5 视觉实现计划

**版本**: v2.0
**日期**: 2026-04-07
**状态**: 进行中 - 遮罩层映射完成，待集成到游戏
**前置条件**: Phase 1功能性验收通过
**参考文档**:
- [视觉设计规范 v1.0](../specs/2026-04-05-visual-design-v1.0.md)
- [黑白遮罩层自动映射设计](../specs/2026-04-07-mask-to-config-design.md) ⭐最新

---

## 🎉 重大进展：黑白遮罩层自动映射完成（2026-04-07）

### 核心成果

**已完成工作:**
1. ✅ 实现黑白遮罩层自动映射系统 (`mask_to_config.py`)
2. ✅ 成功识别可行走区域：916个瓦片 (22.2%)
3. ✅ 精确定位3个大门：药园(15,8)、诊所(60,8)、家(61,35)
4. ✅ 定位交互点：水井(43,24)
5. ✅ 自动修正家门路径连通问题
6. ✅ 输出完整配置文件和可视化验证图

**技术路线变更:**
- ❌ 旧方案：AI分块识别（失败，效率低、不准确）
- ✅ 新方案：黑白遮罩层像素分析（成功，精确可控）

### 地图规格更新

| 项目 | 旧值 (Phase 1) | 新值 (Phase 1.5) |
|-----|---------------|-----------------|
| 地图像素尺寸 | 1280×960 | **2752×1536** |
| 瓦片网格 | 40×30 | **86×48** |
| 瓦片大小 | 32×32 | 32×32 (不变) |

### 门位置识别结果

| 门 | 瓦片坐标 | 像素坐标 | 场景 | 状态 |
|-----|---------|---------|------|------|
| 药园 | **(15, 8)** | (495, 271) | GardenScene | ✅ 可达 |
| 诊所 | **(60, 8)** | (1947, 269) | ClinicScene | ✅ 可达 |
| 家 | **(61, 35)** | (1967, 1149) | HomeScene | ✅ 可达（已修正） |

### 交互元素

| 元素 | 瓦片坐标 | 像素坐标 | 类型 |
|-----|---------|---------|------|
| 水井 | **(43, 24)** | (1395, 787) | 可交互 |

### 输出文件

```
tests/visual/asset-test/ai-generated/town_new/mask_analysis/
├── alignment_check.png     # 尺寸对齐检查
├── walkable_overlay.png    # 可行走区域可视化
├── connectivity_grid.png   # 连通域网格图
├── map_config.json         # JSON配置
├── town-walkable-config.ts # TypeScript配置
└── analysis_report.json    # 分析报告
```

---

## ⚠️ 失败尝试记录

### ❌ AI分块识别瓦片坐标

**方案**: 将全景图切割成多个小块（如10×8=80个区域），使用多模态AI逐块识别元素类型。

**失败原因**:
1. **效率低下**: 80个区域需要80次AI API调用
2. **识别不准确**: AI对小块图片识别结果不一致，边界判断模糊
3. **融合困难**: AI生成的素材与背景图融合感差
4. **成本高昂**: 大量API调用，时间和经济成本都很高

**相关文件**: `tile_mapping_v2.py`, `tile_mapping_v3.py`, `tile_mapping_v4.py`

### ❌ Tile-by-Tile逐瓦片识别

**失败原因**: 瓦片数量巨大（86×48=4128个），单个瓦片信息不足。

### ❌ AI直接输出坐标

**失败原因**: 多模态模型坐标能力弱，图片压缩导致坐标偏移。

### ✅ 正确方向

**「AI画遮罩，脚本算坐标」**: AI负责生成黑白遮罩层标注，自动化脚本负责像素分析和坐标计算。

---

## 1. 阶段概述

### 1.1 阶段目标

在Phase 1功能性骨架基础上，完成视觉设计实现，让玩家能够真正"走进完整的游戏世界"，感受田园治愈+中医文化+神秘探索的综合氛围。

### 1.2 阶段划分（更新）

```
Phase 1.5: 游戏世界视觉呈现
├── 1.5.1 视觉设计规范评审 ✅ 已完成
├── 1.5.2 视觉验收标准制定 ✅ 已完成
├── 1.5.3 AI生成流程搭建 ✅ 已完成
├── 1.5.4 AI vs 手工对比测试 ✅ 已完成（决策：混合方案）
├── 1.5.5 小镇外景全景图生成 ✅ 已完成
│   └── new_town_nanobanana3.jpeg (2752×1536)
├── 1.5.6 黑白遮罩层生成 ✅ 已完成
│   └── new_town_shadow2.jpeg
├── 1.5.7 遮罩层自动映射 ✅ 刚完成 ⭐
│   ├── 可行走区域识别：916瓦片
│   ├── 门位置识别：3个
│   ├── 路径连通修正：家门路径扩展
│   └── 配置文件输出
├── 1.5.8 游戏集成 ⏳ 下一步
│   ├── 更新地图尺寸和碰撞配置
│   ├── 加载原图作为背景
│   └── 基于配置实现碰撞检测
├── 1.5.9 室内场景适配 ⏳ 待开始
├── 1.5.10 端到端功能验证 ⏳ 待开始
└── 1.5.11 用户体验验证 ⏳ 待开始
```

### 1.3 下一步行动

1. **更新游戏地图配置**: 将新的地图尺寸(86×48)和门坐标集成到 `map-config.ts`
2. **实现背景图加载**: 在TownOutdoorScene中加载 `new_town_nanobanana3.jpeg`
3. **实现碰撞检测**: 基于可行走瓦片配置实现碰撞层
4. **更新场景切换**: 使用新的门坐标

---

## 2. 技术架构更新

### 2.1 新的分层架构

```
┌─────────────────────────────────────────────────────────────┐
│  UI层        │ 场景名称、提示文字、交互提示                   │
├─────────────────────────────────────────────────────────────┤
│  玩家层      │ 玩家角色Sprite                               │
├─────────────────────────────────────────────────────────────┤
│  交互层      │ 门(红色标记)、水井(黄色标记)等交互点           │
├─────────────────────────────────────────────────────────────┤
│  碰撞层      │ 基于遮罩分析的可行走区域                       │
│              │ (916个可行走瓦片，不可见)                      │
├─────────────────────────────────────────────────────────────┤
│  背景层      │ AI生成的小镇全景图                            │
│              │ new_town_nanobanana3.jpeg (2752×1536)        │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 碰撞检测方案

**基于可行走瓦片列表:**
```typescript
// 从遮罩分析得出的可行走瓦片坐标
const WALKABLE_TILES = new Set([
  {x: 15, y: 8},  // 药园门附近
  {x: 60, y: 8},  // 诊所门附近
  {x: 61, y: 35}, // 家门附近
  // ... 916个瓦片
]);

// 碰撞检测
function isWalkable(tileX: number, tileY: number): boolean {
  return WALKABLE_TILES.has(`${tileX},${tileY}`);
}
```

### 2.3 门配置更新

```typescript
// 新的门配置（基于遮罩分析）
const DOORS = [
  {
    id: 'garden',
    tileX: 15,
    tileY: 8,
    targetScene: 'GardenScene',
    spawnPoint: { x: 10, y: 5 }
  },
  {
    id: 'clinic',
    tileX: 60,
    tileY: 8,
    targetScene: 'ClinicScene',
    spawnPoint: { x: 7, y: 6 }
  },
  {
    id: 'home',
    tileX: 61,
    tileY: 35,
    targetScene: 'HomeScene',
    spawnPoint: { x: 5, y: 5 }
  }
];
```

---

## 3. 详细实现计划（更新）

### 3.1 Task 1.5.8: 游戏集成 ⏳ 下一步

**目标**: 将遮罩分析结果集成到游戏代码中

#### 步骤1: 更新地图配置

- [ ] 更新 `map-config.ts` 地图尺寸 (40×30 → 86×48)
- [ ] 更新门坐标配置
- [ ] 添加可行走瓦片列表

#### 步骤2: 更新TownOutdoorScene

- [ ] 加载 `new_town_nanobanana3.jpeg` 作为背景
- [ ] 更新相机边界 (1280×960 → 2752×1536)
- [ ] 实现基于可行走瓦片的碰撞检测
- [ ] 更新门交互位置

#### 步骤3: 更新室内场景出生点

- [ ] ClinicScene: 更新从室外进入时的出生点
- [ ] GardenScene: 更新从室外进入时的出生点
- [ ] HomeScene: 更新从室外进入时的出生点

#### 步骤4: 更新玩家出生点

- [ ] 确定新的玩家初始位置（建议中心区域）
- [ ] 更新 `playerSpawnPoint` 配置

**验收标准**:
- 游戏正常启动，显示小镇全景背景
- 玩家只能在可行走区域移动
- 门交互正常工作，可进入室内场景

---

### 3.2 Task 1.5.9: 室内场景适配

**目标**: 适配室内场景与新的室外场景切换

| 场景 | 当前状态 | 需要更新 |
|-----|---------|---------|
| ClinicScene | 基于旧坐标(7,8) | 适配新出生点 |
| GardenScene | 基于旧坐标(33,8) | 适配新出生点 |
| HomeScene | 基于旧坐标(6,26) | 适配新出生点 |

---

### 3.3 Task 1.5.10: 端到端功能验证

**测试用例**:

| 测试ID | 测试内容 | 验证方法 | 期望结果 |
|-------|---------|---------|---------|
| E2E-001 | 背景图加载 | 截图验证 | 小镇全景正确显示 |
| E2E-002 | 玩家移动 | 键盘操作 | 仅在可行走区域移动 |
| E2E-003 | 药园门交互 | 走到(15,8)按空格 | 进入GardenScene |
| E2E-004 | 诊所门交互 | 走到(60,8)按空格 | 进入ClinicScene |
| E2E-005 | 家门交互 | 走到(61,35)按空格 | 进入HomeScene |
| E2E-006 | 碰撞检测 | 尝试穿墙 | 无法穿过障碍区域 |
| E2E-007 | 水井交互 | 走到(43,24)附近 | 显示交互提示 |

---

## 4. 素材文件清单

### 4.1 已有素材

| 文件 | 路径 | 尺寸 | 用途 |
|-----|------|------|------|
| 小镇全景图 | `town_new/new_town_nanobanana3.jpeg` | 2752×1536 | 室外背景 |
| 黑白遮罩层 | `town_new/new_town_shadow2.jpeg` | 2784×1536 | 碰撞数据来源 |

### 4.2 生成的配置文件

| 文件 | 路径 | 用途 |
|-----|------|------|
| JSON配置 | `mask_analysis/map_config.json` | 程序读取 |
| TypeScript配置 | `mask_analysis/town-walkable-config.ts` | 游戏代码 |
| 可视化验证图 | `mask_analysis/walkable_overlay.png` | 人工复核 |

---

## 5. 阶段验收标准

### 5.1 当前阶段完成标志

- [x] 黑白遮罩层自动映射系统实现完成
- [x] 可行走区域识别完成
- [x] 门位置精确定位完成
- [x] 路径连通性问题修正完成
- [x] 配置文件输出完成
- [ ] 游戏集成完成
- [ ] 端到端测试通过
- [ ] 用户体验验证通过

### 5.2 Phase 1.5 完成标志

- ✅ 玩家可以真正"走进完整的游戏世界"
- ✅ 感受到田园治愈+中医文化+神秘探索的综合氛围
- ⏳ 小镇全景背景正常显示
- ⏳ 碰撞检测基于真实可行走区域
- ⏳ 场景切换正常工作

**后续阶段**: Phase 2 NPC系统开发

---

*本计划更新于 2026-04-07，记录遮罩层映射重大进展*